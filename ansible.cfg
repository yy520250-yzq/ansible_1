[defaults]
# 本地跑时的默认清单;在 Semaphore/AWX 里执行时以平台配置的 Inventory 为准
inventory = inventory/hosts.yml

# 首次连接陌生主机若卡在 host key 确认,可取消注释(只适合内网测试环境,生产不建议)
# host_key_checking = False

# 可读性更好的输出
stdout_callback = yaml
