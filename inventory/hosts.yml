---
# 机器清单:把真实服务器信息按注释格式填进去即可
all:
  children:
    linux:                    # 例子:一组 Linux 服务器
      hosts:
        # web01:
        #   ansible_host: 192.168.1.10
        #   ansible_user: root
        #   ansible_become: true
        # db01:
        #   ansible_host: 192.168.1.11
        #   ansible_user: ubuntu
        #   ansible_become: true
    local:                    # 本地(本机)测试用
      hosts:
        localhost:
          ansible_connection: local
